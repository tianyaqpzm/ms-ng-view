// In Angular 21 with @angular-builders/jest:run, the builder handles TestBed initialization automatically.
// Explicit calls to setupZoneTestEnv() or setupZonelessTestEnv() here will cause 
// "Cannot set base providers because it has already been called" errors.
